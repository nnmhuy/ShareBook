
'use strict';

const fs = require('fs');
const path = require('path');

module.exports = function() {
  // try {
  //   fs.mkdirSync('../image-storage');
  // } catch (err) {
  //   if (err.code !== 'EEXIST') throw err;
  // }
};
