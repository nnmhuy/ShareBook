{
  "db": {
    "host": "${DB_HOST}",
    "port": "${DB_PORT}",
    "url": "",
    "database": "${DB_NAME}",
    "password": "${DB_PASSWORD}",
    "name": "db",
    "connector": "mongodb",
    "user": "${DB_USERNAME}",
    "useNewUrlParser": true
  }
}
